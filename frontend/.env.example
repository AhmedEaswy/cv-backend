# Laravel API base URL for the Nuxt app.
# With Laragon + scripts/laragon/cv.test.conf, use https://cv.test.
# Without the proxy, use http://localhost:8000 (php artisan serve).
NUXT_PUBLIC_LARAVEL_URL=https://cv.test
NUXT_PUBLIC_APP_NAME=CV
NUXT_PUBLIC_APP_STORE_URL=https://apps.apple.com/us/app/cv-resume-builder/id6744250877
NUXT_PUBLIC_PLAY_STORE_URL=https://play.google.com/store/apps/details?id=app.cv.creator

# Sidebase Auth.js (Google) — same pattern as digi-pedia.
# MUST include /api/auth (not the site root) or sidebase recurses on /session.
# Google Cloud Console → Authorized redirect URIs:
#   {NUXT_AUTH_ORIGIN}/callback/google
# e.g. https://cv.test/api/auth/callback/google
# LinkedIn sign-in is handled by Laravel at /auth/linkedin/redirect (not Auth.js).
# Apple sign-in is handled by Laravel at /auth/apple/redirect (not Auth.js).
# Set to false to hide buttons (also set LINKEDIN_AUTH_ENABLED / APPLE_AUTH_ENABLED in Laravel .env).
# Accepted falsy values: false | 0 | off | no
NUXT_PUBLIC_LINKEDIN_AUTH_ENABLED=true
NUXT_PUBLIC_APPLE_AUTH_ENABLED=true
NUXT_GOOGLE_CLIENT_ID=
NUXT_GOOGLE_CLIENT_SECRET=
NUXT_AUTH_SECRET=
NUXT_AUTH_ORIGIN=https://cv.test/api/auth
NUXT_SITE_URL=https://cv.test
