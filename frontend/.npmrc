# Allow esbuild's postinstall script (required for the native binary).
# pnpm 11+ blocks install scripts by default; this whitelists the known-safe ones.
enable-pre-post-scripts=true
# Deploy runs over SSH without a TTY; don't prompt when node_modules must be rebuilt.
confirmModulesPurge=false
